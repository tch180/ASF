import React, { Component } from 'react';

class streams extends Component {
    render() {
        return (
            <div>
                Hello from streams. 
            </div>
        );
    }
}

export default streams;